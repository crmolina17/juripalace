<!-- Fixed navbar -->
<nav class="navbar navbar-expand-lg navbar-dark fixed-top" id="mainNav">
    <div class="container">
        <button aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation" class="navbar-toggler" data-target="#navbarResponsive" data-toggle="collapse" type="button">
            <i class="fas fa-bars"></i>
        </button>
        <a class="navbar-brand js-scroll-trigger" href="#page-top">{{translation._title}}</a>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav text-uppercase ml-auto">
                <li class="nav-item">
                    <a class="nav-link js-scroll-trigger" href="{{translation.menuItems.menu1.link}}">{{translation.menuItems.menu1.title}}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link js-scroll-trigger" href="{{translation.menuItems.menu2.link}}">{{translation.menuItems.menu2.title}}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link js-scroll-trigger" href="{{translation.menuItems.menu3.link}}">{{translation.menuItems.menu3.title}}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link js-scroll-trigger" href="{{translation.menuItems.menu4.link}}">{{translation.menuItems.menu4.title}}</a>
                </li>
            </ul>
            <ul class="navbar-nav ml-auto">
                <li class="nav-item dropdown">
                    <a class="nav-link text-uppercase dropdown-toggle" href="{{translation.menuItems.menu5.link}}" id="navbarDropdown" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">{{translation.menuItems.menu5.title}}</a>
                    <div aria-labelledby="navbarDropdown" class="dropdown-menu" role="menu">
                        <a class="dropdown-item {{ lang=='en'? 'disabled' : '' }}" href="{{translation.menuItems.menu6.link}}">
                            {{translation.menuItems.menu6.title}}</a>
                        <a class="dropdown-item {{ lang=='es'? 'disabled' : '' }}" href="{{translation.menuItems.menu7.link}}">
                            {{translation.menuItems.menu7.title}}</a>
                    </div>
                </li>
            </ul>
        </div>
        <ul class="nav navbar-nav text-uppercase navbar-right">
            <li class="nav-item">
                <a class="nav-link js-scroll-trigger" href="#" data-toggle="modal" data-target="{{translation.menuItems.menu8.link}}">|
                    <i class="fas fa-sign-in-alt fa-fw"></i>
                    {{translation.menuItems.menu8.title}}</a>
            </li>
        </ul>
    </div>
</nav>
